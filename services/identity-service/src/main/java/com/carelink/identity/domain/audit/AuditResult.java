package com.carelink.identity.domain.audit;

/** Resultado de la operación auditada — FR-CLN-13: "si la operación falla, registra result = ERROR". */
public enum AuditResult {
    SUCCESS,
    ERROR
}
