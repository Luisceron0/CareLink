package com.carelink.clinical.domain.value;

/** FR-CLN-03 — "urgencias y consulta externa" del SRS §5.4, sin más variantes todavía. */
public enum AdmissionType {
    URGENCIAS,
    CONSULTA_EXTERNA
}
